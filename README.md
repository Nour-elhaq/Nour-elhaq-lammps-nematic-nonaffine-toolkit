# Nour-elhaq-lammps-nematic-nonaffine-toolkit
Add-on LAMMPS input workflow that computes nematic alignment S(t)/S(z) from quaternions and non-affine displacement during shear, plus shear-rate sweep automation.
